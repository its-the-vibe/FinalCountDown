services:
  app:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./config.yaml:/config.yaml:ro
    environment:
      - CONFIG_PATH=/config.yaml
      - LISTEN_ADDR=:8080
    read_only: true
    restart: unless-stopped
